userIdentifier:
    name: userIdentifier
    in: path
    description: Identifier of the user that contact will belong to. Should be either id or username of the user
    required: true
    type: string

contactId:
  name: contactId
  in: path
  description: Id of the contact
  required: true
  type: string