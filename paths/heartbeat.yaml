/v2/heartbeats/{name}/ping:
  get:
    tags:
      - heartbeat
    summary: Ping Heartbeat
    description: Ping Heartbeat for given heartbeat name
    operationId: ping
    externalDocs:
      description: For more information
      url: https://docs.opsgenie.com/docs/rest-api/heartbeat-api#pingHeartbeat
    # x-collapse-params: PingHeartbeatRequest
    parameters:
      - $ref: '#/parameters/HeartbeatName'
    responses:
      '202':
        $ref: '#/responses/Accepted'
      '401':
        $ref: '#/responses/Unauthorized'
      '404':
        $ref: '#/responses/NotFound'
      default:
        $ref: '#/responses/GeneralError'

/v2/heartbeats:
  post:
    tags:
      - heartbeat
    summary: Create Heartbeat
    description: Create a new heartbeat
    operationId: createHeartbeat
    externalDocs:
      description: For more information
      url: https://docs.opsgenie.com/docs/rest-api/heartbeat-api#addHeartbeat
    # x-collapse-params: CreateHeartbeatRequest
    parameters:
      - name: body
        in: body
        description: Request payload of created heartbeat
        required: true
        schema:
          $ref: '#/definitions/CreateHeartbeatRequest'
    responses:
      '201':
        description: Heartbeat information for created heartbeat name
        schema:
          $ref: '#/definitions/CreateHeartbeatResponse'
        headers:
          X-Request-ID:
            description: Id of given request
            type: string
          X-Response-Time:
            description: Process time of given request
            type: string
          X-RateLimit-State:
            description: Status of rate limit
            type: string
          X-RateLimit-Reason:
            description: Source of the rate limit
            type: string
          X-RateLimit-Period-In-Sec:
            description: Time of throttle in seconds
            type: string
      '401':
        $ref: '#/responses/Unauthorized'
      default:
        $ref: '#/responses/GeneralError'

/v2/heartbeats/{name}:
  get:
    tags:
      - heartbeat
    summary: Get Heartbeat
    description: Returns heartbeat with given name
    operationId: getHeartbeat
    externalDocs:
      description: For more information
      url: https://docs.opsgenie.com/docs/rest-api/heartbeat-api#getHeartbeat
    # x-collapse-params: GetHeartbeatRequest
    parameters:
      - $ref: '#/parameters/HeartbeatName'
    responses:
      '200':
        description: Heartbeat information for given heartbeat name
        schema:
          $ref: '#/definitions/GetHeartbeatResponse'
        headers:
          X-Request-ID:
            description: Id of given request
            type: string
          X-Response-Time:
            description: Process time of given request
            type: string
          X-RateLimit-State:
            description: Status of rate limit
            type: string
          X-RateLimit-Reason:
            description: Source of the rate limit
            type: string
          X-RateLimit-Period-In-Sec:
            description: Time of throttle in seconds
            type: string
      '401':
        $ref: '#/responses/Unauthorized'
      '404':
        $ref: '#/responses/NotFound'
      default:
        $ref: '#/responses/GeneralError'

  patch:
    tags:
      - heartbeat
    summary: Update Heartbeat (Partial)
    description: Update Heartbeatwith given name
    operationId: updateHeartbeat
    externalDocs:
      description: For more information
      url: https://docs.opsgenie.com/docs/rest-api/heartbeat-api#updateHeartbeat
    # x-collapse-params: UpdateHeartbeatRequest
    parameters:
      - $ref: '#/parameters/HeartbeatName'
      - name: body
        in: body
        description: Request payload of update heartbeat action
        schema:
          $ref: '#/definitions/UpdateHeartbeatRequest'
    responses:
      '200':
        description: Heartbeat meta information for given heartbeat name
        schema:
          $ref: '#/definitions/UpdateHeartbeatResponse'
        headers:
          X-Request-ID:
            description: Id of given request
            type: string
          X-Response-Time:
            description: Process time of given request
            type: string
          X-RateLimit-State:
            description: Status of rate limit
            type: string
          X-RateLimit-Reason:
            description: Source of the rate limit
            type: string
          X-RateLimit-Period-In-Sec:
            description: Time of throttle in seconds
            type: string
      '401':
        $ref: '#/responses/Unauthorized'
      '404':
        $ref: '#/responses/NotFound'
      default:
        $ref: '#/responses/GeneralError'

  delete:
    tags:
      - heartbeat
    summary: Delete Heartbeat
    description: Delete heartbeat with given name
    operationId: deleteHeartbeat
    externalDocs:
      description: For more information
      url: https://docs.opsgenie.com/docs/rest-api/heartbeat-api#deleteHeartbeat
    # x-collapse-params: DeleteHeartbeatRequest
    parameters:
      - $ref: '#/parameters/HeartbeatName'
    responses:
      '200':
        $ref: '#/responses/Ok'
      '401':
        $ref: '#/responses/Unauthorized'
      '404':
        $ref: '#/responses/NotFound'
      default:
        $ref: '#/responses/GeneralError'

/v2/heartbeats/{name}/enable:
  post:
    tags:
      - heartbeat
    summary: Enable Heartbeat
    description: Enable heartbeat request with given name
    operationId: enableHeartbeat
    externalDocs:
      description: For more information
      url: https://docs.opsgenie.com/docs/rest-api/heartbeat-api#enableHeartbeat
    # x-collapse-params: EnableHeartbeatRequest
    parameters:
      - $ref: '#/parameters/HeartbeatName'
    responses:
      '200':
        description: Heartbeat meta information for given heartbeat name
        schema:
          $ref: '#/definitions/EnableHeartbeatResponse'
        headers:
          X-Request-ID:
            description: Id of given request
            type: string
          X-Response-Time:
            description: Process time of given request
            type: string
          X-RateLimit-State:
            description: Status of rate limit
            type: string
          X-RateLimit-Reason:
            description: Source of the rate limit
            type: string
          X-RateLimit-Period-In-Sec:
            description: Time of throttle in seconds
            type: string
      '401':
        $ref: '#/responses/Unauthorized'
      '404':
        $ref: '#/responses/NotFound'
      default:
        $ref: '#/responses/GeneralError'

/v2/heartbeats/{name}/disable:
  post:
    tags:
      - heartbeat
    summary: Disable Heartbeat
    description: Disable heartbeat request with given name
    operationId: disableHeartbeat
    externalDocs:
      description: For more information
      url: https://docs.opsgenie.com/docs/rest-api/heartbeat-api#disableHeartbeat
    # x-collapse-params: DisableHeartbeatRequest
    parameters:
      - $ref: '#/parameters/HeartbeatName'
    responses:
      '200':
        description: Heartbeat meta information for given heartbeat name
        schema:
          $ref: '#/definitions/DisableHeartbeatResponse'
        headers:
          X-Request-ID:
            description: Id of given request
            type: string
          X-Response-Time:
            description: Process time of given request
            type: string
          X-RateLimit-State:
            description: Status of rate limit
            type: string
          X-RateLimit-Reason:
            description: Source of the rate limit
            type: string
          X-RateLimit-Period-In-Sec:
            description: Time of throttle in seconds
            type: string
      '401':
        $ref: '#/responses/Unauthorized'
      '404':
        $ref: '#/responses/NotFound'
      default:
        $ref: '#/responses/GeneralError'
